<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Style for ImageView. -->
    <style name="TaconeoStore" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">0.70</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <!-- Common style for company logo and description. -->
    <style name="SubCabecera" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">0</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:paddingTop">@dimen/padding_top</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
    </style>

    <!-- Specific style for company logo. -->
    <style name="TaconeoLogo" parent="SubCabecera">
        <item name="android:layout_width">match_parent</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <!-- Specific style for company description. -->
    <style name="TaconeoDescription" parent="SubCabecera">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">@dimen/taconeo_description</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

    <!-- Style for contact info view group (LinearLayout). -->
    <style name="LinearLayout2" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1.5</item>
        <item name="android:layout_marginTop">@dimen/activity_top_margin</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_marginBottom">@dimen/activity_vertical_margin</item>
    </style>

    <!-- Style for contact info icons (drawables) and texts (TextViews). -->
    <style name="TaconeoInfo" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:drawablePadding">@dimen/padding_top</item>
        <item name="android:paddingTop">@dimen/padding_top</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:textColorLink">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/taconeo_info</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>
